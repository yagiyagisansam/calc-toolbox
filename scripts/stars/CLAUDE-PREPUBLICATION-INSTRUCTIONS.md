# Claudeへの作業指示書 — 公開前ブロッカーの解消

対象リポジトリ: `yagiyagisansam/calc-toolbox`  
対象ブランチ: `claude/stargazing-spot-site-uijopc`

最初に最新を取得し、次を全文読んでください。

1. `scripts/stars/FINAL-PREPUBLICATION-REVIEW.md`
2. `scripts/stars/REVIEW-BRIEF-4.md`
3. `scripts/stars/spot-candidates.json`
4. `scripts/stars/selection-2026-08-15.json`

## 0. 厳守事項

1. **公開しない。** mainへのマージ、gh-pagesへのpush、デプロイは禁止。
2. `claude/stargazing-spot-site-uijopc` 以外のブランチへpushしない。
3. **Supabaseへ書き込まない。** 候補のinsert、approve、update、deleteを実行しない。
4. service_roleキー、管理トークンをリポジトリ、生成物、テスト出力、ログへ書かない。
5. 座標、自治体、夜間可否を推測で埋めない。確定できなければ未確定のままにする。
6. Open-Meteoと第三者サイトを繰り返し取得しない。
7. 料金・予約条件の構造化列は追加しない。Hiroさんの決定は「`caution` に有料・公式サイト確認を表示する」である。
8. `acceptance.exceptions` を消したり、検査を一律に緩めたりしない。

## 1. 最優先: 公開対象を安全に切り分ける

`selection = 採用` と「今すぐ公開できる」を同一視しないでください。候補JSONには採用判断と未解決事項が同居しています。

次を機械判定する**公開準備検査**を追加してください。既存の `check_candidates.mjs` は候補選定の履歴検査なので、目的を変えず別検査にします。

公開停止条件:

- `coordVerified !== true`
- `cityCheck.ok !== true`
- `night` が `不可`
- `acceptance.exceptions` に `coordinate` または `city` が残る
- 有料・要予約・要申請・期間閉鎖なのに、その事実と「公式サイトを確認」が `caution` にない

出力は、公開可能件数、停止件数、各停止理由を人が読める形にしてください。停止条件を例外承認だけで成功扱いにしないでください。

少なくとも以下を停止対象として検出するテストを追加します。

- 椿山森林公園: 夜間不可のため掲載対象から外す
- 大川山キャンプ場: 香川県仲多度郡まんのう町中通1156-163を基準に座標を取り直す
- 輝北うわば公園キャンプ場: 鹿児島県鹿屋市輝北町市成1660-3を基準に座標を取り直す
- 大山まきばみるくの里: 鳥取県西伯郡伯耆町小林2-11を基準に座標を取り直す

夜間未確認8件はHiroさんが公開すると決定しました。`acceptance.exceptions` の履歴は残し、夜間未確認を理由に公開停止や注意文追加をしないでください。推測で「夜間可」に書き換えることも禁止です。

住所から座標を取る際は、住所検索結果だけで確定せず、施設公式情報または公式地図と照合してください。確定できなければ「確定できなかった」と報告し、公開可能扱いにしません。3件を直し終えてから、指示どおり国土地理院逆ジオコーダを1回だけ実行し、期待自治体との一致を確認してください。食い違いを自動修正しないでください。

## 2. 地図と一覧に `caution` を表示する

DB列は増やしません。既存の公開RPCが返す `caution` を、そのまま次へ表示してください。

- `stars/app.js` の地図スポットカード
- `stars/list.js` の各スポット行

要件:

- スポット名・点数だけを見て出発判断する前に読める位置に置く。
- `textContent` で表示し、HTMLとして解釈しない。
- 文面から料金額や予約状態を正規表現で推測して別データ化しない。
- 空の `caution` は何も表示しない。
- 長文でも390pxで横にはみ出さず、表の可読性を保つ。
- 詳細ページの既存表示は残す。

モックデータを使い、地図カードと一覧で次が見えるテストを追加してください。

- 「有料。利用料金・予約条件は公式サイトを確認してください」
- 「要予約」または「要申請」
- 冬季・期間閉鎖

## 3. 説明文を新しい掲載範囲へ合わせる

`stars/list.html:153` の「掲載しているのは公共の場所です」は、有料キャンプ場・天文台プログラム・要申請施設を含む現在の方針に合いません。

私有地・農地・牧草地へ無断で入らない注意は残し、例えば次の意味になるよう修正してください。

- 掲載先には駐車場、展望台、公園のほか、有料・要予約・要申請の施設を含む。
- 各施設の公式サイトと現地掲示を出発前に確認する。
- 許可された範囲・時間以外へ立ち入らない。

文言は断定を増やさず、候補ごとの `caution` と矛盾させないでください。

## 4. 候補から登録データを作る「乾式」処理を用意する

現在、候補JSONは公開DBへつながっていません。**DBへ送信しない**変換・確認処理だけを追加してください。

要件:

- 入力は `spot-candidates.json`。
- 公開準備検査を通った採用候補だけを対象にする。椿山森林公園を除く期待件数は30件。停止対象を黙って含めない。
- `name`, `pref`, `city`, `lat`, `lon`, `access`, `note`, `caution`, `source_url` 等を既存 `stars_spots` スキーマへ明示的に対応づける。
- `region`, `name_kana`, `elevation_m`, `facilities` 等、候補に確定値がない項目は推測で生成しない。既存スキーマ上の必須条件を確認し、足りないなら停止理由として出す。
- 変換結果には元候補名、Hiroさんの選択番号、採用日を監査用に対応づけられるようにする。ただしDBスキーマを勝手に増やさない。
- SQLを生成する場合も実行しない。管理トークンやservice_roleキーを含めない。
- 同じ入力から常に同じ結果が出ること、重複名・重複座標・必須項目欠落を検出すること。
- `--dry-run` を既定にし、ネットワークアクセスを行わない。

生成物をリポジトリへ残す場合は、手動確認用であることと「実行禁止・Hiroさんの最終承認が必要」を先頭に明記してください。

## 5. 0件を公開前検査で失敗にする

通常のサイト動作として0件表示は壊さないでください。一方、公開前検査では `stars_public_spots()` が0件、または乾式登録データの期待件数と一致しない場合に失敗する読み取り専用チェックを用意してください。

- 通常テストはモックでよい。
- 実環境チェックはanonキーによる読み取りだけ。
- 書き込みRPCを呼ばない。
- ネットワーク未許可・設定なしの場合は、失敗と未実行を区別して報告する。

## 6. 地図警告を確認する

実ブラウザで MapLibre が `circle-11` のmissing image警告を出しました。スタイルのどのレイヤーが要求しているか確認し、記号欠けがあれば修正してください。外部スタイル由来で表示影響がない場合は、根拠と確認したズーム範囲を報告し、無理に握りつぶさないでください。

## 7. 検証

最低限、次を実行してください。

```text
node scripts/run_tests.mjs
node scripts/stars/sky.test.mjs
node scripts/stars/net.test.mjs
node scripts/stars/places.test.mjs
node scripts/stars/check_candidates.mjs
node scripts/stars/glow.test.mjs
node scripts/stars/land_grid.mjs
node scripts/stars/verify.mjs --width=390
node scripts/stars/verify.mjs --width=768
node scripts/stars/verify.mjs --width=1280
bash scripts/stars/pgtest.sh
bash scripts/stars/setup.test.sh
bash scripts/stars/migrate.test.sh
```

依存不足で実行できないものは、成功扱いにせず理由を明記します。PostgreSQLテストは利用可能な環境で公開前に必須です。

ブラウザでは次を確認します。

- 地図カード・一覧・詳細の3か所で `caution` が同じ意味で見える。
- 390 / 768 / 1280pxで横スクロールが出ない。
- 空の申請は送信されない。
- IDなし・不正IDの詳細ページが安全にエラー表示する。
- 公開DBが空の間は0件表示、登録後の読み取り確認では最終承認数を表示する。
- コンソールエラーと警告を記録する。

## 8. Hiroさんの決定事項

次は確認済みです。再質問せず、このとおり実装・報告してください。

1. 椿山森林公園は掲載対象から外す。
2. 大川山キャンプ場、輝北うわば公園キャンプ場、大山まきばみるくの里は、指定住所を基準に座標を取り直す。
3. 夜間未確認8件は公開し、その理由だけで注意事項を追加しない。
4. 一部の先行公開はしない。椿山森林公園を除いた30件を確定してから初回公開する。
5. この作業では公開もSupabase登録も行わない。乾式登録データの内容を報告し、実行は次の明示指示を待つ。

## 9. 報告形式

「全部直りました」ではなく、次を項目ごとに報告してください。

- 変更したファイルと行
- 各P0/P1への対応結果
- 公開準備検査の対象件数、通過件数、停止件数、停止理由
- 地図・一覧・詳細での注意表示の確認結果
- 乾式登録データの件数と項目対応表
- 実行した検証と結果
- 実行できなかった検証と理由
- 公開DBの読み取り結果（書き込みはしない）
- 確定できなかった事項
- 新たに確定できなかった事項（既決定事項を再質問しない）
- 「公開操作・Supabase書き込みを行っていない」ことの明記

修正をcommitし、pushする場合も対象ブランチだけにしてください。公開・DB登録はHiroさんの明示承認後の別作業です。
